import { Inter, Roboto } from "next/font/google";

const inter = Inter({
    subsets: ["latin"],
    display: "swap",
    variable: "--inter-font",
});

const roboto = Roboto({
    subsets: ["latin"],
    weight: "400",
    display: "swap",
    variable: "--roboto-font",
});
